micronaut:
  application:
    name: demo
netty:
  default:
    allocator:
      max-order: 3
  security:
    authentication: bearer
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: changeThisSecret
